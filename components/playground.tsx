"use client";
import { SlidersHorizontal, Sparkles } from "lucide-react";
import { tokenize } from "@/math/attention";
import { useStudioStore } from "@/stores/studio-store";
export function Playground() { const { input, setInput, positionalEncoding, setPositionalEncoding, heads, setHeads, layers, setLayers } = useStudioStore(); const count = tokenize(input).length; return <section className="playground-panel"><div className="playground-head"><div><span className="eyebrow"><Sparkles size={13}/> LAB CONTROLS</span><h2>Make the model yours.</h2></div><SlidersHorizontal size={20}/></div><label>Input sentence <textarea value={input} onChange={(event) => setInput(event.target.value)} maxLength={90}/><small>{count} tokens · updated live</small></label><div className="control-row"><div><span>Positional encoding</span><small>Preserve token order</small></div><button className={positionalEncoding ? "toggle on" : "toggle"} onClick={() => setPositionalEncoding(!positionalEncoding)} aria-pressed={positionalEncoding}><i/></button></div><label className="range-label">Attention heads <b>{heads}</b><input type="range" min="1" max="8" value={heads} onChange={(event) => setHeads(Number(event.target.value))}/></label><label className="range-label">Encoder layers <b>{layers}</b><input type="range" min="1" max="12" value={layers} onChange={(event) => setLayers(Number(event.target.value))}/></label></section>; }
